<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    
    <!-- Daily Reset Service Permissions -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    
    <!-- Bluetooth Permissions for Thermal Printer -->
    <uses-permission android:name="android.permission.BLUETOOTH" 
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" 
        android:maxSdkVersion="30" />
    <!-- Android 12+ Bluetooth Permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />

    <application
        android:name=".PosApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/cafe_hmpage"
        android:label="Loreta's Café"
        android:roundIcon="@mipmap/cafe_hmpage_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.Loretacafepos"
        tools:targetApi="31">

        <!-- Main Activity (Login Screen) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Reset Password Activity (Email Entry) -->
        <activity
            android:name=".ResetPasswordActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".MainActivity" />

        <!-- OTP Verification Activity -->
        <activity
            android:name=".OtpVerificationActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".ResetPasswordActivity" />

        <!-- New Password Activity -->
        <activity
            android:name=".NewPasswordActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".OtpVerificationActivity" />

        <!-- Password Success Activity -->
        <activity
            android:name=".PasswordSuccessActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Dashboard Activity -->
        <activity
            android:name=".DashboardActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Menu Activity -->
        <activity
            android:name=".MenuActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".DashboardActivity" />

        <!-- Recent Transactions Activity -->
        <activity
            android:name=".RecentTransactionsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".DashboardActivity" />

        <!-- Transaction Detail Activity -->
        <activity
            android:name=".TransactionDetailActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".RecentTransactionsActivity" />

        <!-- Categories Activity -->
        <activity
            android:name=".CategoriesActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".DashboardActivity" />

        <!-- Assign Items Activity -->
        <activity
            android:name=".AssignItemsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".CategoriesActivity" />

        <!-- Edit Item Activity -->
        <activity
            android:name=".EditItemActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".DashboardActivity" />

        <!-- Recipe Editor Activity -->
        <activity
            android:name=".RecipeEditorActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".EditItemActivity" />

        <!-- Create Order Activity -->
        <activity
            android:name=".CreateOrderActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".DashboardActivity" />

        <!-- Order Summary Activity -->
        <activity
            android:name=".OrderSummaryActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".CreateOrderActivity" />

        <!-- Inventory Activity -->
        <activity
            android:name=".InventoryActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".DashboardActivity" />

        <!-- Terms and Conditions Activity -->
        <activity
            android:name=".TermsAndConditionsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".MainActivity" />

        <!-- Sales Report Activity -->
        <activity
            android:name=".SalesReportActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".DashboardActivity" />

        <!-- Printer Settings Activity -->
        <activity
            android:name=".PrinterSettingsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".DashboardActivity" />

        <!-- User Management Activity (Admin Only) -->
        <activity
            android:name=".UserManagementActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".DashboardActivity" />

        <!-- Shift Management Activity -->
        <activity
            android:name=".ShiftManagementActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".DashboardActivity" />

        <!-- FileProvider for Camera -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.loretacafe.pos.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Daily Reset Service Receiver -->
        <receiver
            android:name=".util.DailyResetService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
